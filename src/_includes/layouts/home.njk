{% extends 'layouts/base.njk' %}

{# Intro content #}
{% set introSummary %}
  {{ content | safe }}
{% endset %}

{# Post list content #}
{% set postListHeading = postsHeading %}
{% set postListItems = collections.postFeed %}

{# Archive link #}
{% if postListItems.length < collections.posts.length %} 
  {% set paginationNextText = archiveButtonText %}
  {% set paginationNextUrl = '/archive' %}
{% endif %}

{% block content %}
  <main class="prose md:prose-lg lg:prose-xl mx-auto py-6 px-4 sm:p-6 md:py-10 md:px-8">
    <article>
      <h1>Hi there! 👋</h1>
      <p>
        I'm Rob Dodson, a web developer passionate about climate change.
      </p>
      {% include "partials/post-list.njk" %}
      {% include "partials/pagination.njk" %}
    </article>
  </main>
{% endblock %}
